# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of control
# version systems.
*.tfvars
*.tfvars.json

# Override files which are usually used to override main.tf locally.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl mechanism
# It is recommended to include lock file in version control, but some teams
# prefer to exclude it. Uncomment the following line to exclude it.
# .terraform.lock.hcl

# Go modules
modules.json
